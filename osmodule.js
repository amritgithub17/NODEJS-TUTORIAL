const os = require('os');

console.log(os.freemem()); //tells the freememory
console.log(os.homedir()); //tells the home directory
console.log(os.hostname()); //tells the hostname
console.log(os.platform()); //tells the paltform like : win32
console.log(os.release()); //tells the release of OS 

